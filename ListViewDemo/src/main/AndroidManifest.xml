<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listviewdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/listview_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/listview_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppListActivity" />
        <activity android:name=".RequestDataActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".SimpleListViewActivity"></activity>
    </application>

</manifest>